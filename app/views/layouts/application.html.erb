<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Opendesk" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Opendesk">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100">
    <% if authenticated? && managing_organisation? %>
      <div class="bg-gray-900 text-white text-sm">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 flex items-center justify-center gap-3 h-10">
          <span>Managing <strong><%= Current.organisation.name %></strong></span>
          <%= button_to organisation_session_path, method: :delete, class: "inline-flex items-center gap-1.5 px-2.5 py-1 text-xs font-medium rounded-md border border-gray-600 text-gray-300 hover:text-white hover:border-gray-400 transition-colors cursor-pointer" do %>
            <%= lucide_icon("log-out", class: "h-3.5 w-3.5") %>
            Stop Managing
          <% end %>
        </div>
      </div>
    <% end %>
    <nav class="bg-white">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-16">
          <div class="flex items-center gap-8">
            <%= link_to root_path, class: "flex items-center gap-1.5" do %>
              <%= image_tag "logo.svg", class: "h-5 w-5" %>
              <span class="font-semibold text-gray-900 tracking-tight">OpenDesk</span>
            <% end %>
            <% unless authenticated? %>
              <div class="flex items-center gap-1 text-sm">
                <%= link_to "Home", root_path, class: "px-3 py-2 rounded-md font-medium text-gray-900 bg-gray-100" %>
                <%= link_to "About", "#", class: "px-3 py-2 rounded-md font-medium text-gray-500 hover:text-gray-900 hover:bg-gray-50 transition-colors" %>
                <%= link_to "Pricing", "#", class: "px-3 py-2 rounded-md font-medium text-gray-500 hover:text-gray-900 hover:bg-gray-50 transition-colors" %>
              </div>
            <% end %>
          </div>
          <div>
            <% if authenticated? %>
              <%= button_to session_path, method: :delete, class: "p-2 rounded-full hover:bg-gray-100 transition-colors" do %>
                <%= lucide_icon("circle-user-round", class: "h-5 w-5 text-gray-600") %>
              <% end %>
            <% else %>
              <%= render "shared/button", label: "Sign in", href: new_session_path %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>
    <% if authenticated? && managing_organisation? %>
      <%= render "shared/tables_navbar" %>
    <% end %>
    <% flash.each do |type, message| %>
      <%= render "shared/toast", type: type, message: message %>
    <% end %>
    <%= yield %>
  </body>
</html>
